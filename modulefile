#%Module1.0

# The Singularity container for dvctools.
set SIMG /share/apps/ngs-ccts/simg/dvctools-0.2.simg

# The version of Singularity that we have tested.
module load Singularity/2.4.1-GCC-5.4.0-2.26

# Aliases to make interacting with the container less verbose.
# What is a better way to keep track of default bind points?
set-alias git "singularity exec --bind /data $SIMG git"
set-alias dvc-fork "singularity exec --bind /data $SIMG python3.6 /app/fork-new-project.py"
